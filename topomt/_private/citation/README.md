In any place the following lines can be used:

```python
@scoped_usage("topomt.features.detect_pockets")
def detect_pockets(...):
    track_item("topomt:2024:concavity")
```

And nothing happens if the library flowcite is not installed.

